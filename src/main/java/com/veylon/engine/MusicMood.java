package com.veylon.engine;

/** Read-only scene observations selecting a sparse musical phrase, never simulation state. */
public enum MusicMood {
    CALM, NIGHT, THREAT, DEEP_CAVE, FIRST_NIGHT, BEACON
}
